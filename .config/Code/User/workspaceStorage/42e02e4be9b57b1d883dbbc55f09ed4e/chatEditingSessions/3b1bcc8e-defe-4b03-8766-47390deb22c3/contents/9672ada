#!/bin/bash
# Outputs the path to the logo image or ASCII art

# Directory with logos
LOGO_DIR="/home/phitter/.config/fastfetch/logo"

# Pick a random .icon file in the directory
RANDOM_LOGO=$(find "$LOGO_DIR" -type f -name "*.icon" | shuf -n 1)

# Output it
echo "$RANDOM_LOGO"

